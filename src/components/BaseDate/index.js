import React, { Component } from 'react';

export default class BaseDate extends Component {
  render() {
    return (
      <div>
        <div>기준일</div>
        <div>Date()</div>
      </div>
    );
  }
}
